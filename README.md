```
pkg up
pkg upgrade
pkg install python
pip install futures
pip install bs4
pip install httpx
pip install mechanize
git clone https://github.com/Alienrazor-Hash/File-Cloning/
pkg install git
cd File-Cloning
python Run.py
```
